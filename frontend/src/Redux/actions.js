/*------------------- Actions -------------------------- */

export const setCurrentUser = (userObj) => ({type: "SET_CURRENT_USER", payload: userObj})

/*------------------- Thunk -------------------------- */
